# Copy to .env.local and add your key to enable live PDF extraction.
# The key is used only server-side (in the /api/extract route) and is never
# exposed to the browser. The sample-report flow works without a key.
ANTHROPIC_API_KEY=sk-ant-...
